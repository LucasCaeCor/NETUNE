rules:
  - id: no-http-in-service
    message: "Service nao pode usar HTTP"
    severity: ERROR
    languages: [typescript]

    pattern-either:
      - pattern: res.json(...)
      - pattern: res.send(...)
      - pattern: res.status(...)

    paths:
      include:
        - "**/services/**"