rules:
  - id: CS-003-no-business-logic-in-controller
    message: "Lógica de negócio não pode ficar no controller"
    severity: ERROR
    languages: [typescript]

    pattern: |
      if ($COND) {
        ...
      }

    paths:
      include:
        - "**/controllers/**"